git log master..code-review             unterschied zwischen zwei branches
git diff master..code-review

git rebase master                       commits auf neuen master raufballern --> fast forward bei merge

git push <remote_name> --delete <branch_name>    auf origin delete löschen

git branch -a                           remote und lokale branches anzeigen                 

git prune <remote>                      entfernt alte branches

git remote -v                           remotes anzeigen
